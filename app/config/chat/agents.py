# TODO: 
# We should fill this in with a function that returns a agent_alias_id and agent_id
# when given a hazard name and foundational model, using boto3. Will need to update IAM roles for server
# This allows client to dynamically select model and hazard.